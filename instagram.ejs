<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>instageram page</title>

    <style>
      img {
        width: 300px;
      }
    </style>
  </head>
  <body>
    <h1>THis page belong to @<%= data.name %></h1>
    <button>follow</button>
    <button>mesaage</button>

    <p>
      Followers: <%= data.followers %> &nbsp; &nbsp;&nbsp; Following: <%=
      data.following %>
    </p>

    <hr />

    <% for(let post of data.posts){ %>
    <img src="<%= post.image %>" alt="some img" />
    <p>
      Likes: <%= post.likes %> &nbsp;&nbsp;&nbsp; coomments:<%= post.comments %>
    </p>
    <% } %>
  </body>
</html>
